language: node_js
node_js:
  - "0.12"
  - "0.10"
  - "iojs"

script:
  if $LINT ; then npm run lint ; else npm test; fi

env:
  matrix:
    - LINT=true
    - LINT=false
matrix:
  fast_finish: true
  allow_failures:
    - env: LINT=true

notifications:
  email: false
